<?php

/*

Here, the permissions that each role has must be stored, if a permissions is commum in more than one role
it should be repeated in each role, example:

return [
    UserRolesEnum::ADMIN => [
        'dashborad index',
        'dashboard sales_report,

        'users index'
        'users block_users'
    ],

    otherRole => [
        'dashborad index',
    ]
]

*/

return [

];
